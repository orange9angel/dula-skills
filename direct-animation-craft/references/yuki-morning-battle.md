# 小雪《早起大作战》：从几何体角色到可修型角色

审阅日期：2026-09-21。案例包括原集诊断与随后完成的独立二维修型试片；原集未整体重制。

## 已检查的证据

路径均相对工作区根目录：

- `dula-story/episodes/yuki_morning_battle/output/output.mp4`：ffprobe 确认 62 秒、1920×1080、30fps。
- 从该视频实际解码 22s 和 49s 帧；临时检查图位于该 episode 的 `tmp/craft_review_22s.png`、`tmp/craft_review_49s.png`，可重新抽取，不作为永久资产依赖。
- 辅助看过 `output/ep1_check.jpg`、`storyboard/check_shot_08.jpg`、`storyboard/check_shot_15.jpg`；预览构图与成片抽帧存在差异，因此诊断不只依赖 storyboard。
- 阅读该集 `bootstrap.js`、`script.story`、`VISUAL_REVIEW.md`，以及 `dula-assets/characters/Yuki.js`。
- 首轮诊断未完成连续动态播放评审；不能从这些静帧断言整段动作滑步、运行帧率错误或所有姿态都失效。后续试片的验证范围见本文末尾。

## 观察与原因

| 问题 | 实际依据 | 升级方向 |
|---|---|---|
| 球手，缺少可读手势 | `Yuki.js` 的 `handGeo = new THREE.SphereGeometry(0.05, 16, 16)`；22s 成片可见圆形手部 | 先做放松、指向、张手、握拳，持物镜头再补抓握；统一腕口与手掌方向 |
| 胳膊像短棒，腿像直柱 | build 为单段 capsule；后续 `RigAdapter` 会自动补关节并重新挂接 mesh，但不拆分或蒙皮这块胶囊 | 检查实际关节树，重塑可连续弯曲的皮肤和宽窄变化，再验证极限姿势 |
| 足部缺少脚形 | episode 将扁球鞋换成同轴 capsule 靴；22s 图仍缺少清楚的脚背/鞋头转折 | 设计能辨认脚跟、鞋头和底面的形体，配踝部及接触控制 |
| 接缝、眼周、发梢出现碎边 | 成片抽帧可见；bootstrap 对角色调用 `sketchify` 并添加 12Hz boil | 干净风格用稳定外轮廓、减少内部接缝线；若保留速写也需控制碎线幅度 |
| 材质处理不一致 | 基础皮肤和衣服使用 `MeshToonMaterial`，替换靴子使用 `MeshStandardMaterial` | 造型成立后统一鞋与角色的色块、阴影规则；不能仅凭材质类型判定最终观感 |
| 技术补丁多，但缺少美术目标样板 | review 多轮解决裙底、鞋袜断口、姿态翻转；没有看到本集手脚多视图和弯曲样板 | 先建立少量能指导制作的姿势样板，再逐镜验收 |

`ShoeFixedYuki` 的“同轴不可能错位”是局部补丁注释，不是通用保证。build 阶段腿、袜、鞋同属一个 leg group，但 `CharacterBase` 构造函数随后调用 `RigAdapter.adapt()`，按局部位置把部分网格移到膝和踝下；只读 build 会漏掉这一点。适配器不会自动分割单块皮肤或建立蒙皮。接缝形状、描边壳和适配后的父子/局部变换需要分别检查。

历史 review 把袜鞋边残留归为手绘质感，只代表当时的记录。本轮用户明确提出手脚和线条质量不足，这个反馈应成为新的验收依据。

## 旧 skill 需要纠正的边界

- “只许基础几何体、不能导入模型”适用于原来的程序原型方式，不能成为最终角色升级的限制。
- “商业二维一律固定 12fps”不成立。本集 30fps 输出下固定 12Hz 会形成不等长帧保持；应先确定曝光设计。这里发现的是规范风险，未断言本集骨骼动作已经按 12Hz 运行；bootstrap 的 12Hz 参数直接用于 boil。
- “所有手都固定切换、禁止关节手指”限制了近景抓握和展开动作，应按镜头需求选择。
- “胶囊鞋解决错位”不能上升为鞋的设计标准；“不穿模”也不能作为轮廓美观的充分条件。

## 建议的最小试片

下列是首轮制定的实施方案。后续已按其中的手势和迈步目标完成局部试片，范围见末节。默认以干净二维电视动画为方向；若用户另有参考，保持同一诊断而调整线条与造型目标。

1. **统一小雪样板**：沿用双马尾、服装和识别特征，补正面、四分之三侧面、侧面的手腕/脚踝设计；选择中性站姿、弯肘指向、迈步支撑三个压力姿势。
2. **手臂与手的试片**：以 48.4–51.4s 的“给我出去”作为语义参考，使用能看清手的机位做指向、停顿、回收。先验证拇指、食指方向、肘腕曲线，再放回原镜头检查遮挡。比较造型时保留同机位基线。
3. **腿脚试片**：以 21.6–25.0s 的移动段为参考，完成至少一个支撑—摆动—落地周期，显示膝弯曲、踝部与鞋头；以真实步幅匹配根位移。脚和阴影分离的静帧可能是腾空，需用连续片段判断是否漂浮。
4. **线条整理**：先看无描边剪影，再加稳定轮廓；保留必要的袜口和袖口信息，去掉由零件拼接造成的无意义黑圈。肤色、鞋、衣服按同一光色设计处理。
5. **成片验证**：导出前后对比、关键姿势图和连续短片；检查 30fps 下实际帧保持、手型过渡、脚底接触以及直接 seek 与连续推进的差异。

成功标准是关键动作可读、手脚轮廓成立、弯曲和接触稳定、角色符合样板。样片通过后，再把这套资产与动作方式推广到完整剧集。

优先考虑保留已有三维场景与摄影机，为角色增加连续形体和镜头修型；若只做固定机位表演，也可转为可变形的二维分层角色。两条路线都需要真正的新资产和动态检验，不能通过修改 skill 文案本身完成。

## 首次试用的实际结果

用户随后要求试用 skill，已产出 `dula-story/episodes/yuki_morning_battle/craft_trial/`：

- 原三维卧室 + 二维关节驱动角色，重画连续手型、四肢轮廓、鞋形、脸与服装。试片不代表三维蒙皮路线已经实现。
- 两个动作取原剧本第 15/8 条的语义及时间窗，共 6.4s；导出 1080p/30fps 的无声对比版和单独试片。对比版左侧是原模型的新动作压力测试，不是原片摘录。
- 每种输出完整顺序渲染 192 帧，支撑脚世界坐标漂移为 0，腿长误差在浮点精度内；11 个关键帧的顺序渲染与直接 seek 哈希相同。
- 检查了实际关键姿势、剪影和从成片解码的动作图。用户随后明确认可“效果好非常多”；全局转面、前景遮挡、完整跑步和对白口型尚未实现。
- 初版站姿把膝盖的弯曲全投在画面平面内，造成蛙腿；调整站姿髋高后改善，但用户仍指出“罗圈腿”。此项为 P1 未解决，用户要求先同步、后续再修，不能记录为已验收通过。

可复用的教训：检查构造后的运行时绑定；从实际镜头设计关节弯曲平面；保持支撑脚、身体位移和曝光时钟一致；用有边界的可运行试片验证工艺。

下一轮沿用当前已认可画面基线，集中检查 `craft_trial/pose.js` 的 `solveKnee`、髋膝踝在镜头中的排列、支撑脚间距及腿部轮廓。不能只凭腿长误差小或脚不滑就判定腿型合格；以同机位站姿、运动极值剪影和连续动作检查为准。保留已经认可的手、脸、服装与线条方向，避免修腿时全盘改造。

## 后续实际修正：连续三维四肢与转台

用户再次要求继续修罗圈腿、罗圈手，并询问能否像原三维模型一样自由转身。于是保留二维基线，在同一 `craft_trial/` 新增 `pose3d.js`、`character3d.js`、`viewer3d.html` / `viewer3d.js`。

- 明确角色局部 +Z 朝前，膝盖朝前弯；自然站姿髋高 0.6925、踝高 0.095，大腿/小腿长度 0.305/0.295，上臂/前臂 0.19/0.18。数值只适用于此 Q 版变体。
- 每条肢体为一片连续截面网格，截面宽厚分开设计；两骨段保持轴线，仅在关节附近约短骨段长度 12% 的区域过渡。调整手掌厚度与放松时的朝向，并衰减腕口接合处的内部描边。
- 复用已认可的眼睛绘制函数作为曲面纹理；头、身体、头发、衣服、手脚具有真实空间表面。侧面与背面按深度遮挡，不依赖正面图层始终朝向镜头。
- 产出 `output/yuki_craft_3d.mp4`：12.4s、1080p、30fps、372 帧，无声。前 6.4s 仍取原剧本第 15/8 条，后 6s 由 `inspection.story` 定义模型转台。`render.mjs --3d` 可重建；`--3d --serve --port 4199` 启动交互视角检查。
- 完整顺序渲染通过，16 个关键帧直接 seek 哈希一致；支撑脚三维漂移约 `1.39e-17`，臂腿长度误差在浮点精度内。看过正侧背面、额外五个角度、剪影与最终视频解码的动作图；旧二维 192 帧检查仍通过。

结论范围：三维试片已针对外拱膝和软管手臂的原因修正，模型可以连续旋转；尚待用户审美复看。转台不是足部锁定的真实原地转身。本变体使用 episode 局部程序变形器，尚未映射 `CharacterBase` 的通用姿态矩阵、替换共享 `Yuki` 或重制原集；刘海接合、领口、手掌体块与强弯曲姿势仍可精修。不能把“模型可转面”写成“所有旧动作已兼容”或“完整动画模型已验收”。
